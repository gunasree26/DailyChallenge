#include<stdio.h>

int main()
{
    int n,sum=0,pp=1;
    scanf("%d",&n);
    while(n>0){
        if(n%2==0)
        pp*=n%10;
        else
        sum+=n%10;
        n/=10;   
    }
    if(pp%sum==0)
    printf("Yes");
    else
    printf("No");
}
