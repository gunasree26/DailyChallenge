#include<stdio.h>
#include<stdlib.h>

int main()
{
    int n,x;
    scanf("%d%d",&n,&x);
    int k,sum=0,ctr=1;
    while(ctr<=n){
        scanf("%d",&k);
        sum+=pow(x,k);
        ctr++;
        
    }
    printf("%d",sum/x);

}
